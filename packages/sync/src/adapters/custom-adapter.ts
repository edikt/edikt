import type { SyncAdapter } from "./adapter-interface";

// Stub — implementation pending
export function createCustomAdapter(_config: Record<string, unknown>): SyncAdapter {
  throw new Error("Not implemented");
}
