import type { SyncReport } from "@edikt/types";

// Stub — implementation pending
export function syncFiles(_sourceFile: string, _targets: string[]): Promise<SyncReport> {
  throw new Error("Not implemented");
}
