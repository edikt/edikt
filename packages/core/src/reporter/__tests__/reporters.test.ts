import { describe, it } from "bun:test";

describe("Reporters", () => {
  it.todo("should format lint results as stylish output");
  it.todo("should format lint results as JSON");
  it.todo("should format lint results as compact output");
  it.todo("should format lint results as GitHub annotations");
});
