import type { PatternMatch } from "@edikt/types";

// Stub — implementation pending
export function searchWithGrep(_pattern: string, _paths: string[]): Promise<PatternMatch[]> {
  throw new Error("Not implemented");
}
