import type { Pattern, PatternMatch } from "@edikt/types";

// Stub — implementation pending
export function searchPatterns(_content: string, _patterns: Pattern[]): PatternMatch[] {
  throw new Error("Not implemented");
}
