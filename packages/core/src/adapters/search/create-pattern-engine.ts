import type { PatternEngine } from "../../ports/pattern-engine";

export async function createPatternEngine(): Promise<PatternEngine> {
  // Full implementation in Prompt 3
  return {
    search: async () => [],
  };
}
