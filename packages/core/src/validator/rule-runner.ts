import type { LintResult, Rule } from "@edikt/types";

// Stub — implementation pending
export function runRules(_rules: Rule[], _filePath: string): Promise<LintResult> {
  throw new Error("Not implemented");
}
