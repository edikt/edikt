import type { DetectedFramework } from "@edikt/types";

// Stub — implementation pending
export function detectFrameworks(_rootDir: string): Promise<DetectedFramework[]> {
  throw new Error("Not implemented");
}
