import type { DetectedLanguage } from "@edikt/types";

// Stub — implementation pending
export function detectLanguages(_rootDir: string): Promise<DetectedLanguage[]> {
  throw new Error("Not implemented");
}
